# memory
Project: CRM REST API (Express 5 + Mongoose 9, TS, CommonJS). Client: ../app.
Entry: server.ts -> connectDB() -> app.listen(env.port). app.ts = etag "weak" + cors() + express.json() + apiCacheHeaders on /api + route mounts.
Caching: middleware/cache.middleware.ts sets GET `private, max-age=0, must-revalidate` + `Vary: Authorization`, non-GET `no-store`. Clients revalidate with If-None-Match against Express ETags.
Scripts: dev=nodemon server.ts (ts-node). No build/start/test script.
Env: PORT, MONGO_URI, JWT_SECRET, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, opt GOOGLE_REDIRECT_URI, opt CLIENT_URL(def http://localhost:3000), MAIL_USER, MAIL_PASSWORD. config/env.js exposes port, mongo_url, jwt_secret, client_url, google_client_id/secret/redirect_uri. Legacy unused CLIENT_ID/CLIENT_SECRET in .env = a different GCP client than GOOGLE_CLIENT_ID.
Mounts: /api/auth, /api/workspaces, /api/workspace-members, /api/modules, /api/collections, /api/columns, /api/records, /api/record-values, /api/api-key.
Auth: middleware/auth.middleware.ts `protect` — Bearer JWT (payload {userId}) first, else x-api-key -> User.apiKey+isActive. Sets req.user.id (AuthRequest).
Tokens: services/jwt.service.ts createToken(userId) 7d. services/apiKey.service.ts generateApiKey() -> `crm_<32B hex>`.
Public routes: POST /api/auth/register, /api/auth/login, GET /api/auth/google, GET /api/auth/google/callback. GET /api/auth/me is protected. All others protected.
Google OAuth: server-side code flow, no SDK dep (global fetch). services/google.service.ts builds consent URL, exchanges code, fetches /oauth2/v3/userinfo. CSRF state = short-lived JWT signed with JWT_SECRET carrying {nonce,mode}. Callback upserts by googleId|email then redirects to `${client_url}/auth/callback#token=` (fragment, never query). Errors redirect with #error=<code>: not_configured|invalid_state|missing_code|no_email|exchange_failed|access_denied.
Route shape: nested resources keyed by parent id on POST/GET (/:moduleId, /:collectionId, /:workspaceId); PUT/DELETE keyed by own id. record-values POST is "/" .
Models: User(googleId, authProvider local|google; password+lastName required only when authProvider=local), Workspace, WorkspaceMember(role owner|admin|member|guest, status active|pending|inactive), Module(visibility private|workspace|public), Collection, Column, Record, RecordValue(value:any), Activity, ApiToken(tokenHash), Comment, File, Notification. All timestamped.
Workspace.icon (2026-08-26): an opaque KEY from the client's catalog (app/lib/workspaceIcons.tsx), trimmed to 40 chars, default "". Deliberately not a URL, class name or emoji — rendering stays the client's business, so the icon set can change with no migration. Accepted by createWorkspace and updateWorkspace. updateWorkspace was ALSO fixed while there: it built its patch from an explicit allowlist (name/description/icon) instead of the previous single hardcoded field, so a caller still cannot rewrite owner or slug.
Hierarchy: Workspace > Module > Collection > Record > RecordValue; Column belongs to Module.
Convention: after any mutation call touchWorkspace(workspaceId) from utils/workspaceHelper.ts. ALSO call touchModule(moduleId) from utils/workspaceHelper.ts on anything module-scoped (2026-08-26). Mongoose timestamps only move when the MODULE DOCUMENT is written, so editing a record, a cell, a collection or a column left Module.updatedAt untouched — a workspace listed 'Customers - 1d ago' seconds after someone renamed a record inside it, which is the bug that surfaced it. Wired into all 14 touchWorkspace sites across record/recordValue/collection/column controllers, each paired with the module id that site already has in scope. Uses updateOne (nothing needs the doc back, and this is the hot path of every cell write).
Naming: controllers/wrokspace.controller.ts is a typo, kept as-is.
Unwired: services/otp.service.ts (nodemailer) has no route/controller; ApiToken model unused by /api/api-key (uses User.apiKey).
tsconfig include omits scripts/** (target es2022, commonjs, strict, outDir ./dist).
Gaps: no global error handler, no input validation lib, no rate limiting, cors() fully open. Not done from REDUX-PLAN.md: flatten the {workspaces:[{workspace}]} envelope (breaking, membership fields would be lost), GET /api/workspaces/:id/bootstrap batch endpoint, record pagination. totalModules already uses one Module.aggregate (no N+1).
Uploads: /api/uploads (routes/upload.routes.ts, all protected) — GET /limits, POST+DELETE /avatar, POST+DELETE /workspace/:workspaceId. multer memoryStorage buffers -> services/cloudinary.service.ts streams to Cloudinary; nothing hits disk. Limits + allowed mime types are the single source of truth in middleware/upload.middleware.ts UPLOAD_LIMITS (avatar 5MB images-only, attachment 10MB x10) and are mirrored client-side in app/lib/imageCompression.ts UPLOAD_RULES — change both together. Server transforms enforce the real ceiling (avatar 512x512 fill/face, attachment 1920 limit, quality auto:good, fetch_format auto). Avatar public_id IS the user id so re-upload overwrites in place; User.avatarPublicId tracks it for delete. googleCallback mirrors profile.picture into Cloudinary best-effort and only when user.avatar is blank (never overwrites an uploaded picture). Env: CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET/FOLDER — added to .env as EMPTY placeholders 2026-08-22, uploads 503 until filled.
Activity log: models/Activity.ts records who/what/where/when + before/after. Field is `collectionName` NOT `collection` (Document.collection is the driver handle — naming it `collection` fails tsc). Write via services/activity.service.ts logActivity() — best-effort, swallows its own errors, never fails the caller's mutation; call it AFTER touchWorkspace. Controllers that do findByIdAndUpdate must read the doc FIRST to capture `before` (the update returns the new doc). Wired: recordValue create/update/delete (cell_updated), record create/update/move/delete, collection + column create/update/delete, module create/delete, member add/remove. NOT wired: workspace create/update/delete, login, comments, file uploads. Read: GET /api/activity/:workspaceId?moduleId=&recordId=&userId=&action=&limit=&before= (keyset paging on createdAt, membership-gated). Retention: env.activity_retention_days / ACTIVITY_RETENTION_DAYS (default 90, 0 = forever). Mongo bakes the TTL window into the index, so syncActivityRetention() runs from server.ts on every boot and drops+recreates index `activity_ttl` when the number changes — verified 90->30->0->90.
Activity revert: POST /api/activity/:workspaceId/:activityId/revert. Rules live ONLY in services/activityRevert.service.ts REVERTIBLE_ACTIONS = cell_updated, record_updated, record_moved, record_deleted, collection_updated, column_updated. Creations are NOT revertible (undo would delete nested data) and hard deletes are NOT (rows gone, `before` holds only the name). The feed adds canRevert + revertBlocker per row so the client never re-implements the rules. Reverting writes its OWN activity row (metadata.revertOf) and stamps the original revertedAt/revertedBy, so a row reverts at most once (second attempt = 409). Guests are blocked. Frontend: ActivityRow has a two-step Revert control, fires window event crm:activity-reverted so the activity page (local accumulated state) reloads; the drawer refreshes via ACTIVITY_TAG invalidation.
Automations (REBUILT 2026-08-26, was 4 triggers / 4 actions / module-only): models/Automation.ts = {workspace, module (NULL for workspace scope), scope "module"|"workspace", name, trigger{type,column,columnName,value}, conditions[{source,column,columnName,field,op,value}], match "all"|"any", actions[{type,column,columnName,collectionName,targetModule,targetCollection,value}], isActive, runCount, lastRunAt, lastError}. 14 TRIGGER_TYPES: record_created|record_moved|record_renamed|record_completed|record_uncompleted|record_archived | column_changed|column_changed_to | subrecord_created|subrecord_column_changed|subrecord_column_changed_to|subrecord_completed|all_subrecords_completed | amendment_posted. 14 ACTION_TYPES: set_column_value|clear_column_value|move_to_collection|archive_record|set_completed|rename_record | create_subrecord|complete_all_subrecords|archive_all_subrecords | set_parent_column_value|set_parent_completed|move_parent_to_collection | create_record | post_amendment. 9 CONDITION_OPS (added not_contains, starts_with). TWO IDEAS CARRY THE WHOLE DESIGN. (1) SCOPE: a module recipe addresses columns by ObjectId; a WORKSPACE recipe has module:null, runs on every module, and addresses them by NAME (`columnName`), resolved per-event against the module the event happened on — an id there would silently narrow it back to one board, which is what the user chose workspace scope to avoid. The two addressing modes are never mixed and scope is FIXED at creation (updateAutomation reads scope from the STORED doc, never the body). (2) SUBJECT: subjectOf(triggerType) in services/automation/triggers.ts says whether a trigger is about a record or a sub-record, and the engine drops any event whose subject disagrees. That is what keeps "when a record is created" off checklist lines now that sub-records DO reach the engine — the old blanket `if (parentRecord) return` is gone. ENGINE SPLIT into services/automation/: index.ts (orchestrator), triggers.ts, conditions.ts, columns.ts (ColumnResolver, per-request name->id cache, anchored+ESCAPED regex), values.ts (str/same/asNumber/fillTemplate), recipe.ts (sanitise+validate, shared with the controller so the two cannot disagree), emit.ts, and actions/{record,subrecord,parent,create}.actions.ts + writeCell.ts behind a HANDLERS registry — nothing switches on action type any more, so an unknown type is skipped rather than throwing. services/automation.service.ts is now a 2-line RE-EXPORT SHIM so every existing import keeps resolving. Unchanged invariants: `void runAutomations(...)` (never awaited), no cascades (actions write models directly), errors land on lastError, activity attributed to the TRIGGERING user with metadata.automation. Every action returns false when the record is already in the requested state — that is what keeps the run feed honest and stops a recipe pointed at its own trigger column from looping. Action `value` supports exactly two placeholders, {record} and {value} (fillTemplate) — richer ones need a resolver with its own failure modes. EMITTERS: emitColumnChange() does ONE parentRecord lookup then fires the correct record-vs-subrecord trigger PAIR (firing all four would double the hot path); emitIfChecklistFinished() decides all_subrecords_completed against the DB after the write, and refuses to fire when the count is zero (a record with no sub-records has not "completed them all"). New emit sites: createSubRecord, updateRecord (rename + completion, completion handled INDEPENDENTLY of the move/rename branches since one PUT can do both), deleteRecord (record_archived, or a checklist re-check when a sub-record is archived), createAmendment (root amendments only — replies would re-fire per message). seedSubColumns + DEFAULT_SUB_COLUMNS moved out of record.controller into utils/subColumns.ts because create_subrecord needs them too. ACCESS: PUT/DELETE now use requireAutomationAccess (middleware/access.middleware.ts), NOT moduleFrom.automationParam — that resolver returns undefined for a workspace recipe (module is null) and every edit to one would 403. The old automationParam entry was DELETED so nothing re-uses the trap. Workspace recipes gate on active workspace membership instead. API: GET+POST /api/automations/workspace/:workspaceId (registered BEFORE the single-segment routes so /workspace/<id> can never be read as a module id), plus GET+POST /:moduleId and PUT+DELETE /:automationId. There is deliberately NO /runs route — see the run-log entry below. Guests blocked; server owns validation and the client shows its message verbatim. create_record is the only action that writes outside the event's module, so it re-checks that the target collection's module belongs to the SAME workspace (a Collection has no `workspace` field — go via Module). VERIFIED by scripts/probe_automation.ts (32/32, self-cleaning throwaway fixture): subject guard both directions, parent roll-up, the three checklist cases (zero children / one outstanding / last one), workspace-scope firing on two different modules from ONE recipe, name mismatch not matching, match all-vs-any, record-field conditions (collection, subrecord_count), cross-module create_record, the cross-WORKSPACE refusal, {record}/{value} templating, run attribution, and paused recipes. Re-run it after touching the engine.
Presence: User.status (online|busy|dnd|away|offline, default online, USER_STATUSES exported from models/User.ts) is only what the user PICKED; User.lastSeen is bumped by the heartbeat. What others see is derived on read by services/presence.service.ts effectiveStatus() — picked status while lastSeen is within PRESENCE_TIMEOUT_MS (150s), else offline; picking "offline" (appear offline) always wins. API: PATCH /api/auth/status {status}, POST /api/auth/heartbeat, and status+presence on GET /api/auth/me (+status on login). getWorkspaceMembers populates status+lastSeen, then STRIPS both and returns only user.presence — the raw pick never leaves the server or "appear offline" would leak. Client mirror of the enum: app/lib/presence.ts — change both together. getWorkspaceModules ALSO derives presence for module.createdBy now (publicCreator() in module.controller.ts): it populates status+lastSeen, derives `presence` via effectiveStatus, and STRIPS both raw fields — same rule as getWorkspaceMembers, so 'appear offline' cannot leak through a module list.
List pagination (opt-in, added for the Data Console): utils/pagination.ts parsePagination/paginationMeta/parseSort. A GET that sends NEITHER `limit` NOR `page` returns the exact legacy shape and pays no countDocuments — that is what keeps the app's own queries working. Sending either adds a `pagination` block {page,limit,total,totalPages,hasMore,nextPage} next to the same array key. limit clamps 1-200 (default 50), page min 1. `sort`+`order` go through parseSort with a PER-ENDPOINT allowlist, so the field name can never be caller-controlled. Wired into: workspaces (pages the membership rows), modules (stats computed for the page only), collections, columns, records, record-values, workspace-members, automations. GET /api/activity keeps its own keyset paging (limit+before+nextCursor) — do NOT convert it to offset paging; new rows land on top and would shift pages.
Role assignment: PUT /api/workspace-members/:workspaceId/:userId {role} -> updateWorkspaceMemberRole. MEMBER_ROLES + MemberRole now EXPORTED from models/WorkspaceMember.ts (least->most privileged: guest, member, admin, owner) and used as the schema enum; client mirror is app/lib/roles.ts. Rules live only in the controller: caller must be an ACTIVE owner|admin of that workspace (read from the token, never the body); only an owner may grant or remove `owner`; a workspace can never drop below one active owner (409-style 400 'last owner'). Logs member_role_changed (already in ACTIVITY_ACTIONS) with before/after. Role-based ACCESS is NOT enforced anywhere yet — the role is only stored; that is the next piece of work.
Board-level access (monday model, enforced server-side): models/ModuleMember.ts = additive grant {module, workspace, user, grantedBy}, unique {module,user}. ALL rules live in services/access.service.ts decideModuleAccess() and NOTHING may re-implement them: not an active member -> nothing; owner|admin -> every board; guest -> ONLY explicit grants whatever the visibility; member -> visibility workspace|public yes, private only if granted or createdBy===them. Enforcement is at the ROUTE via middleware/access.middleware.ts requireModuleAccess(resolver) — resolvers turn a collectionId/columnId/recordId/recordValueId/automationId (every board-scoped model stores `module`) or body.record back into a module id. Guarded: collections, columns, records, record-values, automations (all verbs) + DELETE module. getWorkspaceModules filters by accessibleModuleFilter() so a private board never appears in a list; the activity feed adds an $or so board rows are scoped while workspace-level rows (no module) stay visible. API: GET+PUT /api/module-access/:workspaceId/:userId (owner|admin only) — GET returns per-board {granted, openToRole, canAccess} already decided so the client never re-derives; PUT replaces the grant set, validates every id belongs to THAT workspace, logs module_access_changed (new ACTIVITY_ACTIONS entry). Migration: scripts/backfill_module_access.ts (--dry) grants members every PRIVATE board and guests EVERY board, preserving pre-enforcement access; dry run on this DB reported 0 needed (2 boards, both workspace-visible, no guests).
Board settings: PUT /api/modules/:moduleId {name?,description?,icon?,color?,visibility?} -> updateModule (module.controller.ts). Until 2026-08-24 there was NO update endpoint for a module at all, so visibility could not be changed anywhere — which made the whole access model unusable. Route carries requireModuleAccess(moduleFrom.param); on top of that, changing `visibility` additionally requires owner|admin (it is the lever that decides who can open the board), while name/icon/colour need only board access. Logs module_updated with a message that names the visibility change. Module.tags (string[], indexed, 2026-08-26): free-form labels, accepted by create + update and normalised by sanitiseTags() in module.controller.ts — trimmed, whitespace-collapsed, capped at 12 tags x 24 chars, and de-duplicated CASE-INSENSITIVELY with the first spelling kept ('Client' and 'client' are one tag to a person and two to a database, and a filter that misses half the rows is worse than no filter). On update, `undefined` means 'not sent' while `[]` means 'clear them'.
Automation runs are ACTIVITY ROWS — there is no run log and there must never be one (2026-08-26). GET /api/automations/:moduleId/runs was DELETED: it was a second read path over the exact rows the activity feed already returns, which is how two views of one store drift. GET /api/activity/:workspaceId now takes `source=automation|person` (filters on metadata.automation existing) and labelRuns() attaches `automation {_id,name}` to every automated row in ONE extra query for the page, resolving a deleted recipe to "A deleted automation" because the audit trail outlives the rule. It also STRIPS the leading `automation "<name>" ` from `message` — that prefix exists so the raw row is self-describing in Mongo, but on screen the name belongs in the badge and leaving it in the sentence says it twice. This is still the only way the client learns a recipe ran (the engine is fired with `void` and never awaited) — do NOT make runAutomations awaited to fix that. SYSTEM USERS (2026-08-26): models/User.ts gained `systemKey` (enum SYSTEM_USER_KEYS = automation | announcer, unique+SPARSE — without sparse every real user collides on null) and utils/systemUsers.ts owns systemUserId(key), a process-cached findOneAndUpdate UPSERT so two concurrent runs on a cold process cannot race two bots into existence. A bot has no password, authProvider 'google' (so the model's local-only password rule does not fire), and isActive:false — the flag `protect` already rejects — so it can never hold a session; it is never a WorkspaceMember, so it cannot appear in a roster. The engine's log() now writes user = the automation bot and metadata.triggeredBy = the real user, FALLING BACK to the triggering user if the bot cannot be resolved (a missing bot must never cost the audit row). It also stamps metadata.automationName, so a recipe deleted later still names itself in the feed and the page pays no lookup; labelRuns prefers the stamp, falls back to a lookup for pre-stamp rows, then to 'An automation'. 'announcer' is RESERVED and unbuilt — intended for one broadcast addressed to every member at once instead of a send per person; adding it is a row in SYSTEM_USER_PROFILES, no schema change. Probe now 40/40 and asserts: the bot is created on first use, cannot sign in, is idempotent (exactly one row), owns every automated row, and preserves triggeredBy + automationName.
Atlas agent (chat -> CRM writes): POST /api/agent/chat {messages[],workspaceId?,moduleId?} -> controllers/agent.controller.ts -> services/agent.service.ts (manual tool-use loop on @anthropic-ai/sdk) + services/agentTools.service.ts (12 tools). config/env.js exposes anthropic_api_key / anthropic_model (default claude-haiku-4-5) / anthropic_max_tokens (400) / anthropic_max_steps (4). COST IS THE DESIGN CONSTRAINT — the user is on a $5 budget: Haiku 4.5, no thinking, 400 max_tokens, last 6 turns only, one-line tool descriptions, tool results trimmed to ids+names, and the loop capped at anthropic_max_steps so ONE message cannot run away. The reply carries usage{inputTokens,outputTokens,costUsd,steps} and the panel shows running spend. Do NOT fatten the system prompt or tool descriptions — every word is re-billed on every request. Tools re-check membership + resolveModuleAccess for the CALLING user (a tool is not a bypass) and write through touchWorkspace + logActivity + runAutomations, so a chat change is indistinguishable from a manual one. delete_item returns needs_confirmation unless confirm:true, so a misread sentence cannot drop a board. Errors: provider error bodies are never forwarded (401 -> 503 'key rejected', 429 -> 429, else generic 500).
Atlas delete flow: history reaches the model as PLAIN TEXT (tool_use/tool_result blocks are never replayed), so after 'do you want to delete X?' -> 'yes' the id from the previous turn is GONE. delete_item therefore accepts an id OR an exact name and resolves it via resolveTargetId() — workspace by name across the user's memberships, module by name within context.workspaceId, collection/record/column by name within context.moduleId. Without that the confirm turn failed with 'Invalid id'. Same reason applies to any future tool that has to survive a two-turn exchange: never assume the model still has an id it was given last turn. Agent errors are classified in the controller (401/403 -> 503 key rejected, 429, 400 -> 502 'built a bad request' = OUR bug, 402 credit, 5xx -> 502) and logged server-side with {status,name,message,provider}; the provider error BODY is never returned to the client because it can echo the request, which contains CRM data.
Atlas tools are now 13 = full CRUD: list_/create_ per level, set_cell, update_item, delete_item. update_item is ONE tool covering every kind (workspace|module|collection|column|record) with fields name/color/visibility/description/group — adding five per-kind rename tools would have cost five schemas of billed context. It also MOVES a record (`group`, resolved by id or name within that board) and emits record_moved to the automation engine. Rules match the REST routes: workspace rename needs owner|admin, module visibility needs owner|admin, everything board-scoped needs requireBoard. A Column has no top-level `color` (only statusOptions[].color), so only its name is editable. WHY THIS EXISTS: with no update tool the model proposed deleting a collection and recreating it to 'rename' it — which destroys every record inside. The system prompt now carries an explicit line: rename with update_item, NEVER delete and recreate. Any future gap in the tool surface will produce the same class of destructive workaround; add the tool rather than relying on the prompt alone.
VOCABULARY (product decision 2026-08-25): the CRM's nouns are WORKSPACE > MODULE > COLLECTION > RECORD, plus COLUMN, RECORD VALUE and AMENDMENT (a note on a record; never "update", monday's word for it). Never write board / group / item / cell in anything a user or the model reads — prompts, tool names and descriptions, error strings, UI copy. The monday term map in reference.md is for OUR reading only, to compare features; it is not the product's language. Atlas tools are named accordingly (create_module, create_collection, create_record, update_entity, delete_entity — 'entity' because those two cover all five kinds and 'item' would read as a record). AppliedAction.kind uses 'value', not 'cell'.
Atlas authorisation: NEEDS_AUTHORISATION in agentTools.service.ts = {delete_entity, create_workspace, create_module} — deletes plus the two STRUCTURAL creations. Collections/records/columns/values stay immediate; gating the everyday flow would make the agent tedious rather than safe. The gate is generic and sits at the TOP of executeTool, so a new guarded tool cannot forget to ask. It returns {needs_confirmation, pending:{tool,intent,kind,name,input:{...,confirm:true}}}; runAgent surfaces `pending` on the reply. POST /api/agent/confirm -> runAuthorised() executes that payload WITHOUT a model call — free, and it cannot drift into doing something else on a re-read. The client replaying the payload is safe because executeTool re-checks membership + module access for the caller, so a forged pending grants no reach the user did not already have. isConfirmableTool() gates which tools /confirm will replay at all.
Atlas confirmation, the prompt half: the system prompt must say 'Never ask permission in words. Call the tool — the app shows an Authorise button.' The original wording ('ask first, then call again with confirm true') made the model ask in PROSE, so the gate never fired and no button ever appeared — the gate is useless unless the model actually calls the tool. Batch: delete_entity takes `ids` as well as `id`; a batch loops through the SAME single-item path so every permission check still runs per row, and one bad target is reported in `failed` rather than aborting the rest. If the model still emits one call per target, mergePending() in agent.service.ts collapses same-tool/same-kind pendings into ONE authorise prompt (ids[]); different tools or kinds never merge, because one button must not stand for two different changes.
SAFETY RULE (learned the hard way 2026-08-25): authorisation must NEVER be a field on a tool's input schema. `confirm: {type:"boolean"}` was in delete_entity's schema, so the model simply set confirm:true itself and deleted six collections with no button ever shown — anything in a schema is something the model can and will set. It is now an ExecuteOptions ARGUMENT on executeTool(name, input, context, {authorised}), settable only by server code: runAuthorised() (the /confirm route, after a real press) and the batch loop recursing into its own single-item path. runAgent additionally strips any `confirm` the model invents before executing, and the pending payload sent to the client is stripped of confirm/authorised so it can never re-arm anything. Regression test lives in the transcript: forge confirm:true on delete_entity (single AND batch) plus create_workspace/create_module and assert needs_confirmation with nothing applied.
Blueprint (one-press setup): services/blueprint.service.ts + the create_blueprint tool. The MODEL only writes the plan — one gated call carrying modules[{name,collections[],columns[{name,type,labels}],records[{collection,names[]}]}] and an optional workspace name; building it is plain code in buildBlueprint(), so the whole hierarchy costs ONE model round and the press itself is free. parseBlueprint() validates and CAPS before the preview is drawn (BLUEPRINT_LIMITS: 8 modules, 12 collections + 15 columns per module, 25 records per collection, 250 writes total; unknown column types fall back to text) — so what the user authorises is exactly what gets built, never an optimistic version. Omitting `workspace` builds into context.workspaceId and re-checks membership. Records whose named collection is not in the plan are skipped, never guessed. The pending payload carries the parsed plan so the panel can draw it as a tree.
max_tokens is a CEILING, not a budget — you are billed for what the model writes, so a high cap costs nothing on a short answer. It was set to 400 out of misplaced thrift and that broke create_blueprint: the model wrote a preamble sentence plus a few hundred tokens of plan JSON, hit the cap mid-tool_use, and the truncated call never executed — the turn returned the generic 'I could not finish that'. Now 2000, and stop_reason 'max_tokens' is surfaced as its own message ('that plan was too big to write in one go') instead of dying silently. Measured sizes for reference: all 14 tool schemas = ~1200 input tokens billed EVERY turn (create_blueprint alone ~250); a four-module order-management plan = ~320 output tokens, i.e. right at the old cap once preamble is added. Keep terseness a job for the system prompt, never for max_tokens.
Blueprint workspace field: the schema field is `newWorkspaceName` (was `workspace`, which the model read as 'which workspace' and filled with the id from CONTEXT — the preview showed a chip reading 6A8CBA... and it would have CREATED a workspace named after an ObjectId). parseBlueprint now treats a 24-hex value in that field as the target workspaceId, never as a name, and the gate looks up plan.existingWorkspaceName so the preview says 'Building into <name>' instead of showing a bare id. General rule for tool schemas: a field whose name could be read as either an identifier or a label WILL be filled with an identifier — name it unambiguously and coerce defensively.
NEVER trust context.workspaceId / context.moduleId from the client — they come from the browser and crm_active_workspace_id SURVIVES the workspace being deleted. A stale id made create_blueprint promise 'building into this workspace' on a Home page with no workspaces at all, which would only have failed on the press. The blueprint gate now verifies the target exists AND the caller is still a member, drops it if not, and when nothing is left returns an ERROR AIMED AT THE MODEL ('call again with newWorkspaceName set...') so it recovers inside the same turn instead of surfacing a dead end to the user. Client side, AgentChat only forwards an activeWorkspaceId that is still present in the workspaces list.
Reference/mirror columns — ONE column type, `relation`. Two new Column types + a `settings` subdoc: `relation` {targetModule} links records to records on another module (value = JSON array of RECORD ids, same encoding as person cells, parsed by parseRelationValue) and `reference` {via, field, aggregate} travels one of those relation columns and shows a chosen column's value from the far end. A reference stores NO RecordValue — it is derived on read, which is the whole point: edit the source and every reference is already right, and it is inherently READ-ONLY because there is nowhere to write. services/reference.service.ts resolveModuleReferences(moduleId) resolves the WHOLE board in 4 queries regardless of row count (relation cells -> linked ids -> their names -> their values) and returns recordId->columnId->{items,display,numeric}; the client renders `display` verbatim and never re-derives. Aggregates list|count|sum|avg|min|max: count counts LINKS, the numeric ones ignore blanks, and a sum over a TEXT column is blank rather than 0 (toNumber requires the stripped string to still BE a number — '$1,200' works, 'Open' does not). API: GET /api/record-values/references/:moduleId (two segments, so it cannot be swallowed by GET /:recordId), guarded by requireModuleAccess.
Mirroring is folded into the relation column, NOT split across two like monday does: Column.settings = {targetModule, displayField?, aggregate?}. You pick the module and the column to mirror when creating it; each row then picks a value from that column and it shows against the record. Only the linked record id is stored (JSON array); the value is derived on read by resolveModuleReferences(), which now normalises BOTH shapes into {columnId, via, field, aggregate} — a relation mirrors through ITSELF (via = its own _id, field = settings.displayField), while the retired standalone `reference` type travels settings.via/settings.field. `reference` is no longer offered when adding a column but still resolves, so boards that already have one keep working; do not delete that branch.
Column.settings.targetScope ("record" | "subrecord", default record) decides WHICH SIDE a mirror reads. Record scope is the old behaviour — the linked record's own cell. Sub-record scope reads the cells of that record's CHILDREN, so one link yields one value per sub-record and the aggregate is the point rather than an edge case. resolveModuleReferences pays for it with exactly two extra queries (the children of every linked record, then their values) and ONLY when a sub-scoped mirror exists on the module; the compose step then builds items from the children instead of the linked record. Absent targetScope reads as "record", so every mirror created before this keeps resolving unchanged.
A cell write is IDEMPOTENT: POST /api/record-values on a record+column that already has a row now UPDATES it (logs cell_updated) instead of returning 400 'Value already exists'. That 400 was a silent-data-loss trap — the client only knows to call the update endpoint when the existing row happens to be in its cache, and when it was not (a fresh pick on a cell whose values had not loaded) the write failed, the optimistic patch kept showing the new value, and it vanished on the next refresh. Mirror chain verified on a throwaway fixture: link -> value shows, two links -> both listed, SOURCE edited -> mirror follows with no write on the mirroring board, unlink -> clears.
Mongoose 9: `{ new: true }` on findOneAndUpdate/findByIdAndUpdate is DEPRECATED and floods the dev log with warnings — use `{ returnDocument: "after" }`. All 7 call sites were converted (apiKey, auth x2, record x2, recordValue, workspace); verified the semantics are identical (with the option you get the post-update doc, without it the pre-update doc). Do not reintroduce `new: true` in new code.
Sub-records (monday's subitems): Record.parentRecord (ObjectId ref Record, null on a board row) + Column.scope ("record"|"subrecord"). A sub-record IS a Record — same module, same collectionName, same RecordValue table, same access middleware and the same PUT/DELETE routes — so ONLY two things separate it from a row: getCollectionRecords filters `parentRecord: null`, and its cells are written against subrecord-scoped columns, which is what lets it carry a COMPLETELY different column set from its parent. Every `parentRecord: null` filter also matches the rows written before the field existed (Mongo reads missing as null); the column equivalent is `scope: {$ne:"subrecord"}` — NEVER `scope: "record"`, which would hide every pre-existing column (scopeFilter() in column.controller.ts is the one place that decides this). New: POST+GET /api/records/:recordId/sub-records (two segments, so they cannot be swallowed by the single-segment collection routes) and ?scope=subrecord on GET /api/columns/:moduleId. One level only — createSubRecord 400s on a parent that already has one. The first sub-record on a module SEEDS its sub-columns (Owner/Status/Due date) so the grid is not an empty list of names. Archiving a parent archives its children in the same request (they are reachable only through the row that just disappeared). Sub-records are filtered OUT of the module stats aggregation, the agent's list_records/list_columns and every next-position lookup, and the automation engine returns early for one (a recipe reads "when a record..." and a checklist line is not one) — that check sits AFTER the trigger match so the hot path pays nothing for it.
AMENDMENTS (product name, settled 2026-08-25 — monday calls this an item's Updates tab; we do not, and "update" as a noun is now wrong in user-facing copy). STORAGE KEEPS ITS OLD NAME: models/Comment.ts shipped with the repo, so the Mongo collection stays `comments` and the activity actions stay comment_added / comment_deleted (rows already exist against them) — renaming either is a data migration for zero user benefit. Everything a person reads says amendment. controllers/amendment.controller.ts + routes/amendment.routes.ts mounted at /api/amendments — GET+POST /:recordId (moduleFrom.recordParam), PUT+DELETE /:amendmentId (moduleFrom.amendmentParam, a NEW resolver; Comment stores `module` like every module-scoped model). The two id shapes share one path segment without colliding because no verb is used for both. `parentComment` stays the WIRE field on the reply shape — it is the schema's name and renaming it would buy a mapping layer and nothing else. Reading and posting need only module access; EDITING is author-only even for an owner (moderation may remove someone's words, never change them); DELETING is author OR isWorkspaceManager(req.moduleAccess.role) — requireModuleAccess already put the role on the request, so moderating costs no extra lookup. Soft delete with a TOMBSTONE rule: a deleted amendment is returned (message blanked, user null) ONLY while a live reply still hangs off it, otherwise it is filtered out server-side so the client never has to know the rule; deleting a root takes its replies with it, the same reasoning as archiving a parent record. One level of replies, enforced server-side. Counts: withAmendmentCounts() in record.controller.ts mirrors withSubRecordCounts — ONE grouped aggregate for the whole page, so a 30-row board costs no extra requests; wired into getCollectionRecords only (sub-records have no bubble). An EDIT logs nothing — there is no comment_updated action, so do not invalidate the activity feed for one. NOT built: @mentions and the Notification rows they imply.
Amendments on SUB-RECORDS: getSubRecords now runs withAmendmentCounts, and had to gain .lean() to do it — that helper SPREADS each row, and spreading a hydrated mongoose document copies its internals rather than its fields. A sub-record is a Record, so /api/amendments/:recordId already accepted one; only the count was missing.
DELETE no longer cascades to replies (fixed 2026-08-25). The old `Comment.updateMany({parentComment: id}, {isDeleted:true})` made the tombstone branch of getRecordAmendments UNREACHABLE — it keeps a deleted parent only while a LIVE reply hangs off it, and the cascade had just deleted the reply — and it let the author of a root amendment destroy a colleague's reply by removing their own. Replies now outlive their parent, which renders as a tombstone. The response no longer carries deletedReplies. Found by an end-to-end probe under a throwaway account, not by reading: both halves looked correct in isolation and only cancelled out when run together. Write probes for anything whose read path and write path were authored separately.
Atlas tool `post_amendment` (15th tool): posts an amendment on a record, resolving the record by id OR EXACT NAME through resolveTargetId — history reaches the model as plain text, so a follow-up turn no longer has the id. Deliberately NOT in NEEDS_AUTHORISATION: it is an everyday write like set_cell, it destroys nothing, and the user can delete it in one click — gating it would make the agent tedious rather than safe. It is attributed to the CALLING user, re-checks requireBoard, and writes through touchWorkspace + logActivity (comment_added, message suffixed "via Atlas"); no runAutomations because no trigger reads "an amendment was posted". Its applied action is kind "amendment" but carries the RECORD's id and name, because that is what the reply sentence names and what the panel should chip. The tool description carries the disambiguation ("the user may call it an update or a note. Never a column value") rather than a new system-prompt line — same per-turn billing, better locality. WHY IT EXISTS: with no such tool the model read "post an update on John" as a request to set a COLUMN VALUE and interrogated the user for a collection and a column instead of acting. Same lesson as update_entity: a gap in the tool surface produces a confidently wrong workaround, so add the tool rather than patching the prompt. Also fixed while there: SYSTEM_PROMPT said "Rename or edit with update_item" — a tool that has never existed (it is update_entity).
REALTIME (socket.io, 2026-08-27): server.ts no longer calls app.listen — it builds an http.createServer(app) and hands it to attachRealtime() in services/realtime.service.ts, so /api and /socket.io share ONE port, one origin and one JWT. WHY: two accounts on one board only saw each other's work on a remount, because the client's freshness window is 60s (baseApi refetchOnMountOrArgChange) and nothing told it otherwise; shortening that window would have meant every tab re-fetching every board forever. ONE OUTBOUND EVENT, `crm:change`, carrying a ChangeEvent envelope {entity, action, id, workspaceId, moduleId, collectionId, fromCollectionId, recordId, parentRecordId, columnId, scope, data, actorId, at} — a bespoke event name per entity would be forty listeners that drift; adding an entity is a row in the SCOPE table here and a case in the client's store/realtime.ts. ROOMS: user:<id> / ws:<id> / mod:<id>, names DERIVED server-side from an id the client asks for, never passed in. The SCOPE table decides which room an entity is announced in — board-scoped rows (record, recordValue, collection, column, amendment) go to mod: ONLY, because a cell edit is the highest-frequency event in the product and sending it workspace-wide would bill everyone reading an unrelated board for every keystroke; workspace-scoped rows (module, workspace, member, moduleAccess, automation, activity, presence) go to ws: because the lists they change are drawn outside any one board. AUTH is on the HANDSHAKE (jwt.verify with the same env.jwt_secret! as auth.middleware.ts), so an unauthenticated socket never exists and no handler re-asks. SUBSCRIBE IS A REQUEST, re-checked every time through getMembership / resolveModuleAccess — a board you cannot GET is a board you cannot listen to. VERIFIED by probe: no token / bogus token / wrong-secret token all refused, and a valid user outside a workspace joins zero rooms and receives zero events. ECHO SUPPRESSION: the client sends its socket id as `x-socket-id` on every request (store/baseApi.ts prepareHeaders), controllers read it with originOf(req) and emitChange does io.to(room).except(originId) — excluded at the SERVER so the bytes are never sent, because that client already applied the change optimistically and an echo would fight its own pending patch. Emit sites: record (create/sub-create/update/move/delete), recordValue (x4 incl. the idempotent create branch), collection x3, column x3 (each carries `scope`, since the board grid and the sub-record grid are different cache entries), amendment x3 (each carries parentRecordId, because a sub-record's count lives in getSubRecords(parent)), module x3, workspace update+delete (create is NOT announced — the creator is the only member and their own client has the response), member x3, moduleAccess, automation x4. ACTIVITY IS EMITTED FROM INSIDE logActivity() — one seam covers the whole audit trail and the automation engine, and a controller added tomorrow is live without anyone remembering to wire it; deliberately NO originId there, because unlike a cell edit the actor never wrote that row optimistically. PRESENCE MOVED ONTO THE SOCKET: connections Map<userId, Set<socketId>> (a Set, not a boolean — two tabs are ordinary and closing one must not mark the person offline); first connect bumps lastSeen, last disconnect BACKDATES it past PRESENCE_TIMEOUT_MS so the very next effectiveStatus() read says offline with no window to wait out; a 60s sweep bulk-updates lastSeen for the connected set, which is cheaper than one heartbeat request per user per minute and keeps the HTTP reads (getWorkspaceMembers) honest. effectiveStatus and the lastSeen contract are UNCHANGED — the socket just writes them accurately. announcePresence(userId) is exported and also called from updateStatus in auth.controller.ts, because a PICKED status (busy/dnd/appear offline) is the one presence change no socket lifecycle observes. POST /api/auth/heartbeat still exists for API-key integrators; the app no longer calls it. LIMIT: the connections map is in-memory, so this is SINGLE-PROCESS — a second API node needs @socket.io/redis-adapter and a shared registry, or each node believes it holds the whole truth.
CONEXUS MEET (chat + calls, 2026-08-27): models/Conversation.ts + models/Message.ts, controllers/conversation.controller.ts + message.controller.ts, routes mounted at /api/conversations and /api/messages. WORKSPACE-SCOPED by design: membership, roles and presence are all per workspace here, so a thread spanning them would need a second parallel idea of who may talk to whom, and the first outsider added would be discussing records they cannot open. TWO KINDS, ONE MODEL: a `direct` thread is the degenerate group -- exactly 2 members, no stored name, looked up by its member PAIR ($all plus a $size:2 guard, because $all matches a superset and would otherwise return a group containing both people). openDirect is IDEMPOTENT and that is the point -- clicking a person twice must land in the same thread or the history splits and each person answers in a different copy. UNREAD IS DERIVED from each member's lastReadAt, never stored as a counter: a counter has to be incremented for every member on every send and drifts the first time one of those writes fails. unreadFor() computes it for the WHOLE list in ONE aggregate ($or over per-conversation conditions) -- the obvious countDocuments-per-row is a query per row on a screen that is nothing but rows. lastMessage is denormalised onto the conversation so the list is one query. MESSAGES use KEYSET paging (limit + before + nextCursor), not offset -- a thread grows from the top while you read it, the same reason GET /api/activity was left on keyset. DELETE IS A TOMBSTONE (isDeleted, text cleared) because a reply quoting a vanished message reads as a non-sequitur -- same decision amendments landed on. EDIT is author-only even for a team admin (moderation may remove someone's words, never rewrite them); DELETE is author OR team admin. ATTACHMENTS reuse the existing pipeline verbatim -- uploadAttachmentFiles multer middleware + services/cloudinary.service.ts uploadAttachment, same UPLOAD_LIMITS, same folder scheme; multipart is on its OWN route so a plain JSON send never pays for multipart parsing. A CALL WRITES A ROW into the transcript (Message.system {type: call_started|call_ended|call_missed, callKind, durationMs}) because a call happens inside a conversation and its history belongs where people scroll, not in a second log nobody opens. REALTIME: realtime.service.ts gained conversationRoom() (conv:<id>) and three entities (conversation, message, typing) with a new SCOPE value "conversation". Also gained EmitInput.audience -- deliver to a list of USERS via their user:<id> rooms instead of a shared room, needed because some changes have no room that already holds the right people: a brand-new conversation cannot broadcast to conv:<id> since the other person is not in it precisely BECAUSE they have never seen it, and being removed from a team is the mirror image. subscribe now accepts conversationId, gated on CONVERSATION membership (Conversation.exists members.user) which is NARROWER than workspace membership. meet:typing is relayed and never persisted (a DB round trip per keystroke) and is re-checked against socket.rooms so a client cannot post "X is typing" into any thread by naming its id. meet:signal is a pure WEBRTC RELAY: it never parses SDP, never holds call state, never touches media; every frame is addressed to one user, both ends are re-checked as members of the conversation ($all), and `from` is stamped SERVER-SIDE because a caller-supplied identity would let anyone impersonate anyone in a ring. sendMessage deliberately passes NO originId -- the sender needs its own echo, since that is what replaces its optimistic bubble with the stored row carrying the real id, timestamp and populated sender. VERIFIED by an isolated-fixture probe (36/36, deterministic, throwaway users + workspace created and deleted): idempotent direct threads, live delivery, unread arithmetic both directions, reply/edit/delete permissions, team admin rules, the workspace re-check on invites, outsider blocked from reading/posting/joining/signalling, typing not echoed to sender, tombstones, last-member-leaves deletes the team. Cloudinary image sharing verified against the REAL service (upload 201, res.cloudinary.com URL, kind image, publicId kept, caption preserved, pushed live). PROBE LESSON: the first version picked two REAL members of the busiest workspace and one assertion failed intermittently -- an account with a browser open was marking the thread read the instant the message landed. That was the app working, but it made the test a coin flip and wrote rows into live data. Fixture-isolate any probe over a feature a human might be using at the same time.
MEET GOES CROSS-WORKSPACE + THE ROLE MATRIX (2026-08-27, same day, supersedes the workspace-scoping in the entry above): GET /api/conversations takes NO workspace id and returns every thread the caller is in across every workspace they are an active member of; GET /api/conversations/:workspaceId was DELETED (one read path). WHY: an owner or admin in several workspaces had to leave Meet, switch workspace in the sidebar and come back merely to answer somebody, and a message waiting elsewhere stayed invisible until they happened to switch. NOTHING WAS LOOSENED -- access was never workspace membership, it is membership of the CONVERSATION, which is strictly narrower, so the same rows come back either way. Each row now carries workspace {_id,name,icon} (populated) and myRole, resolved once per page by myWorkspaceRoles() rather than per row. New GET /api/conversations/contacts returns everyone you may start a thread with, GROUPED BY WORKSPACE with per-workspace capabilities -- ONE request instead of a getMembers call per workspace, and the client never re-derives who a guest may message. Declared BEFORE the id routes so the literal segment cannot be read as an id. THE RULES live only in services/meetAccess.service.ts: canStartDirect is true for everyone EXCEPT a guest, who may only open a thread with an owner or admin (a guest sees only the boards shared with them by design, so letting one DM every employee hands them the org chart -- exactly what guest access exists to withhold); canCreateTeam is false for a guest (a team is a way of assembling staff); canManageAnyTeam is owner|admin. canMessageDirectly() looks up BOTH roles itself rather than letting the caller do half of it. canAdministerTeam() in the controller gives TWO ways to qualify -- an admin of the team itself, or an owner/admin of the workspace it lives in (workspace managers already run boards, members and roles; a team they can see but cannot moderate would be the one object that escapes them) -- and only pays for the workspace lookup when the cheap in-document check fails. The contacts endpoint FILTERS a guest's list to owners/admins, and openDirectConversation refuses the same case again: the list is a convenience, never the permission. VERIFIED 21/21 by a two-workspace four-role fixture probe: guest sees one workspace and only owners/admins in it, guest DM to an ordinary member 403s while to an admin it succeeds, guest cannot create a team, member can, a workspace admin AND the owner can both rename a team they did not create while a guest cannot, the owner's list spans both workspaces in one call, a guest's list holds only their own thread, and someone in no workspace gets an empty list rather than an error. FIXTURE GOTCHA: models/Workspace.ts has a UNIQUE, NON-SPARSE slug index, so any probe inserting more than one workspace MUST set slug -- omitting it collides on {slug:null} at the second insert. Both Meet probes now also register their teardown BEFORE creating anything and run it from the catch as well as the happy path; two earlier runs died between creating users and deleting them and left rows in the live database, which is precisely when a probe should not litter.
AI CREDITS + PLANS (2026-08-28): User gained `plan` (ACCOUNT_PLANS free|paid|enterprise, default free, indexed) and `aiCredits` {used, allowanceOverride, periodStart, periodEnd, lifetimeCredits, lastUsedAt}. `plan` is DELIBERATELY NOT the existing `accountType` — that one is the signup funnel's personal|team|organization|other and is what acquisition is segmented on; folding a billing tier into it would have destroyed that data to answer a different question. THE UNIT IS A CREDIT = $0.0001, integer. Money is what actually runs out (one shared key, $5 budget) so the ledger is denominated in it; a MESSAGE quota was rejected because a "hi" and a 4-round create_blueprint differ by an order of magnitude, so it either starves the cheap case or fails to contain the expensive one. Integers because a balance decremented by 0.0035 a few hundred times drifts. services/aiCredits.service.ts is the ONLY place plan rules live: PLANS spec table (allowance from env AI_CREDITS_FREE/PAID/ENTERPRISE = 500/10000/50000 i.e. $0.05/$1/$5 a month, plus per-plan historyTurns 4/6/8 and maxChars 500/1000/2000 — history is pure INPUT cost re-billed every request, the one limit that makes a turn cheaper without making it worse). max_tokens is deliberately NOT per-plan: 400 truncated create_blueprint mid-JSON once already, and a free user would hit exactly that — the honest limit is the budget, not a model crippled into failing incomprehensibly. PERIOD RESET IS LAZY, not a cron: ensurePeriod() rolls it on the read that needs it, filtered on `periodEnd <= now` so two concurrent requests cannot both reset. chargeUsd uses $inc (verified: 5 concurrent charges summed exactly, none lost); creditsForUsd always rounds UP. Gate is in controllers/agent.controller.ts BEFORE the model call — the only point where money can still not be spent — returning 402 (not 403: the request was allowed, the account cannot pay) with the balance attached; /agent/confirm is NOT charged (no model call, already paid for last turn). A failed charge is logged, never thrown: Anthropic billed us either way and a 500 would spend the money and show nothing. setPlan() changes plan AND resets the period — writing `plan` directly carries `used` across, so someone upgrading after spending 500 free credits would get 9,500 of the 10,000 they paid for; scripts/set_plan.ts <email> <plan> [allowance|clear] is the way to do it by hand until billing exists. GET /api/agent/credits + `plan` on /auth/me. Client: AI_CREDITS_TAG in store/baseApi.ts, invalidated by EVERY turn including one that applied nothing (a question with no actions still costs money); AgentChat draws remaining/allowance as a bar, ambers at 20%, and blocks send in the FUNCTION not just the button (Enter and the suggestion chips call it too). VERIFIED by two throwaway-fixture probes plus an end-to-end HTTP run: schema defaults written at register, concurrent charges, lazy roll keeping lifetimeCredits, unknown plan falling back to free, 402 through the real route, override set and cleared, and free exhausting after 15 realistic turns.
